//Ejercicio 3.2: Crea una arrow function que reciba un número y devuelva su cuadrado.

const numeros = [2,4,6];
const dobles = numeros.map(num => num*2);
console.log(dobles);


//El código utiliza el método map() para recorrer el array numeros y aplicar una arrow function que multiplica cada numero por 2, duplicando así los valores. El resultado se guarda en el array dobles, y luego se imprime en la consola.

